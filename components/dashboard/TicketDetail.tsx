"use client"
"use client"
import {useState } from "react"
import { dateToString, numberToRupiah } from "@/helper/convert"
import RoundedImage from "../ui/RoundedImage"

const TicketDetail = ({children}:{children:React.ReactNode}) => {
  return (
    <section>

    </section> 
  )
}


export default TicketDetail
